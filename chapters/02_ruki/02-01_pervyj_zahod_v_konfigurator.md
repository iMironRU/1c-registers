---
status: draft
---

# § 2.1. Открываем конфигуратор: регистр сведений и регистр накопления

Первая часть кончилась. Мы знаем, зачем регистр нужен, из чего он сложен —
измерения, ресурсы, реквизиты — и почему их четыре разных. Всё это мы разобрали
словами, на знакомых вещах, ни разу не открыв конфигуратор.

Теперь открываем.

Дальше и до конца книги вам понадобится работающая платформа: мы будем заводить
регистры, проводить документы и писать запросы. Если платформа ещё не установлена —
поставьте её сейчас, дальше читать без неё бессмысленно. Годится учебная версия и
любая пустая база.

Возьмём два самых простых характера — регистр сведений и регистр накопления — и
пройдём с ними полный круг:

```текст
создать регистр   →   записать в него   →   прочитать ответ
```

Круг займёт всю вторую часть. В этом параграфе — первый шаг: создаём два регистра и
разглядываем, из чего они собраны в конфигураторе. Ни строчки кода: сначала форма,
потом наполнение.

## 1. Три вопроса к «Канцтоварам»

Конфигурация у нас та же, что и в предыдущей книге серии, — **Канцтовары**, маленькая
торговая фирма: ручки, тетради, скрепки. Справочники и документы в ней уже есть:

| справочник | что в нём |
|---|---|
| Склады | Центральный, Запасной |
| Номенклатура | Ручка шариковая синяя, Тетрадь 48 листов |
| Контрагенты | поставщики и покупатели |

Плюс два документа — **Поступление товаров** и **Реализация товаров**.

Если базы под рукой нет, заведите пустую и создайте три справочника: пять минут
работы, по одному полю в каждом. Документы понадобятся позже.

А теперь — самое важное в этом параграфе, и оно не про конфигуратор.

**Регистр не заводят потому, что «пора завести регистр».** Его заводят под вопрос,
на который система должна уметь отвечать. Поэтому начинаем не с дерева объектов, а с
листа бумаги: что у «Канцтоваров» будут спрашивать?

```текст
1.  Какая цена у ручки была 5 марта?
2.  Сколько ручек на Центральном складе сейчас?
3.  Сколько тетрадей продали за март?
```

Три вопроса, и все три обычные — их задаст любой директор любой торговой фирмы.
Посмотрим, что нужно, чтобы на них ответить.

**Ответить из документов можно, но дорого.** Всё нужное в них есть: цена лежит в
строках поступлений, количество — в строках поступлений и реализаций. Но чтобы
получить ответ, придётся перебрать все документы за всё время, отобрать нужные
строки и сложить. На сотне документов это работает, на десятках тысяч — нет.

**Значит, нужен накопитель** — место, куда след ляжет заранее разложенным по тем
разрезам, по которым будут спрашивать. Это и есть регистр.

**Но накопитель какой?** Присмотримся к вопросам ещё раз — они разной породы.

| вопрос | что нужно сделать с записанным |
|---|---|
| «какая цена была 5 марта?» | найти одну подходящую запись |
| «сколько ручек сейчас?» | сложить множество записей со знаком |
| «сколько продали за март?» | сложить множество записей за период |

Первый стоит особняком: цена не копится и не складывается. Она просто меняется во
времени, и в каждый момент действует ровно одна. Второй и третий — родня: оба про
сложение, разница только в границах.

Отсюда и вывод, к которому мы шли: **вопросов три, а регистра понадобится два**. Один
под цену, другой под количество. Их и заведём — по очереди, начиная с более простого.

## 2. Где живут регистры в конфигураторе

Открываем конфигуратор. Слева — дерево объектов конфигурации. Прокручиваем вниз
и видим ветку **Регистры**. Внутри неё — четыре поддерева, по одному на каждый
характер:

```текст
Дерево конфигурации
├── Справочники          ← существительные
├── Документы            ← глаголы
├── …
├── Регистры сведений    ┐
├── Регистры накопления  │  ← сюда ложатся следы
├── Регистры бухгалтерии │
└── Регистры расчёта     ┘
```

Грамматика серии тут проступает буквально: справочники и документы — отдельные
ветки выше, регистры — отдельные ветки ниже. Это не «специальные таблицы под
бухгалтерию», а полноценный класс объектов рядом со всеми остальными.

Во второй части открыты только две ветки из четырёх. Бухгалтерия и расчёт требуют
слов, которых у нас пока нет; до них дойдём в третьей части.

## 3. Первый вопрос: какая цена была на дату

Берём вопрос номер один: **«Какая цена у ручки шариковой была 5 марта?»**

Разберём, что он требует. Цена — величина, которая меняется во времени, и в каждый
момент действует ровно одна. Складывать цены бессмысленно, копить тоже: нам нужна не
сумма, а та единственная, что действовала на нужную дату. Значит, хранить надо не
события, а **значения с датами**, и уметь находить среди них последнее до заданного
момента.

Ровно это делает **регистр сведений** — тот самый фотоальбом. Заводим.

> **Если вы читали «Дерево метаданных 1С».** Эти шаги вы уже проделывали: там мы
> заводили такой же регистр под цены. Пробегите их глазами — руки помнят. Смотреть
> стоит не на клики, а на пометки «почему именно так»: там регистр заводили, чтобы
> узнать, из чего он состоит, здесь — чтобы ответить на конкретный вопрос, и каждое
> решение выводится из него.
>
> **Если не читали** — ничего пропускать не нужно, дальше всё по порядку.

**Шаг 1.** На ветке «Регистры сведений» — правой кнопкой → «Добавить». Конфигуратор
открывает форму свойств нового регистра.

**Шаг 2.** Имя — `ЦеныНоменклатуры`. Под этим именем регистр будет виден и в коде,
и в дереве конфигурации.
Синоним — «Цены номенклатуры», его увидит пользователь.

**Шаг 3.** Свойство **Периодичность** — выбираем «День». Это свойство делает две
вещи: во-первых, у регистра появляется служебное поле **Период** (с точностью до
выбранной периодичности — у нас «День»); во-вторых, это поле автоматически попадает
в ключ уникальности записи. Это и есть «периодический регистр сведений» — тот самый фотоальбом с подписями под
фотографиями. Важное в нём: смотрят не на то, когда фотографию вклеили в альбом, а
на дату в подписи. Так и здесь — важен период записи, а не порядок, в котором записи
добавляли.

> Если поставить периодичность «Непериодический», у регистра не будет поля
> даты — он будет просто хранить актуальное значение по адресу. Для цен это нам
> не подходит: цены меняются во времени, и нам нужна история.

**Шаг 4.** Свойство **Режим записи**. У регистра сведений их два: независимый и
подчинённый регистратору. Выбираем «Независимый» — значит, записи придут не от
документа, а напрямую: обработкой загрузки прайс-листа или руками из формы регистра.
Прайс-лист обычно и приходит файлом, а не рождается из проведения.

**Шаг 5.** Заполняем устройство — те самые три роли: координаты, числа, пометки.

```текст
Регистр сведений «ЦеныНоменклатуры»
├── Измерения
│   └── Номенклатура     СправочникСсылка.Номенклатура
├── Ресурсы
│   └── Цена             Число (15, 2)
└── Реквизиты
    └── (пусто)
```

Реквизитов в этом регистре пока не делаем — они не нужны. Сохраняем регистр (`F7`
или меню «Файл — Сохранить»).

**Что мы получили.** Регистр `ЦеныНоменклатуры` теперь существует. Под капотом
платформа создала под него таблицу в базе, но мы туда напрямую не заглядываем:
разговоров с регистром по-прежнему два — записываем факты и спрашиваем готовый
ответ. Сейчас он пустой, потому что записывать ещё нечего. Заполнять его будем вручную через форму
списка регистра (Предприятие → найти регистр → добавить запись) или отдельной
обработкой загрузки прайс-листа.

**Что делает запись неповторимой.** Раз режим «Независимый» — все координаты плюс
дата:

```текст
координата  Номенклатура = Ручка
дата        5 марта
            ─────────────────────
            такая запись одна
```

У нас координата одна, поэтому пара получилась короткая. Было бы их три — в пару
вошли бы все три плюс дата.

## 4. Второй вопрос: сколько сейчас на складе

Вопросы номер два и три: **«Сколько ручек на Центральном складе сейчас?»** и
**«Сколько тетрадей продали за март?»**

Здесь всё наоборот. Одна запись сама по себе ничего не значит: приход пятидесяти ручек
третьего марта — не ответ, а слагаемое. Ответ получается **сложением множества
записей**, причём со знаком: приход прибавляем, расход вычитаем.

Два вопроса, но накопитель нужен один. «Сколько сейчас» — это сложить всё до
сегодняшнего дня; «сколько продали за март» — сложить расходы между двумя датами.
Записи одни и те же, различаются только границы суммирования.

Складывать со знаком по координатам умеет **регистр накопления**. Его и заводим.

**Шаг 1.** На ветке «Регистры накопления» — правой кнопкой → «Добавить».

**Шаг 2.** Имя — `ТоварыНаСкладах`. Синоним — «Товары на складах».

**Шаг 3.** Свойство **Вид регистра**. Вернитесь к нашим вопросам: второй звучал
«сколько ручек на складе **сейчас**». Это остаток, и держать его умеет вид
«**Остатки**» — его и выбираем. Вид «Обороты» хранит только поток за период; на
«сколько сейчас» такой регистр не отвечает вовсе.

Заметьте, что вид выбран не из четырёх кнопок наугад, а прямо из формулировки вопроса.
Так это и делается.

> Слово «Периодичность» встретится и здесь, но означает другое. У регистра сведений
> оно решало, появится ли у записи дата. Здесь дата есть всегда — её даёт документ,
> задавать отдельно нечего.
>
> А свойство «Периодичность итогов» на закладке «Итоги» — про то, как часто платформа
> сохраняет промежуточные суммы, чтобы быстро отвечать. По умолчанию «Авто», и
> трогать её не нужно.

**Шаг 4.** **Режим записи** у регистра накопления зафиксирован: «Подчинение
регистратору». Никакого «Независимого» режима здесь нет — записи всегда приходят от
документов. Выбор режима есть только у регистра сведений, и мы его только что сделали
на шаге 4 предыдущего регистра.

**Шаг 5.** Заполняем устройство:

```текст
Регистр накопления «ТоварыНаСкладах»   вид: Остатки
├── Измерения
│   ├── Номенклатура     СправочникСсылка.Номенклатура
│   └── Склад            СправочникСсылка.Склады
├── Ресурсы
│   └── Количество       Число (15, 3)
└── Реквизиты
    └── (пусто)
```

Три знака после запятой у количества — на случай, если товар измеряется не в штуках,
а в килограммах. Реквизиты пока не нужны. Сохраняем.

**Что мы получили.** Регистр `ТоварыНаСкладах` теперь существует. Под капотом
платформа создала несколько таблиц: основную таблицу движений и служебную
таблицу итогов. Мы туда напрямую не лезем; в § 2.3 мы научимся писать движения
из документа, а потом — спрашивать у платформы готовый ответ.

**Что делает запись неповторимой.** В регистре накопления это пара «документ-источник
плюс номер строки в нём».
Номер строки платформа ведёт сама: объявлять и заполнять его не нужно, он
появляется сам. Отсюда следует важное — у одного документа может быть много
движений, и каждое знает своего родителя:

```текст
документ «Поступление №14»
   ├── строка 1   Ручка синяя,  склад Центральный,  +50
   ├── строка 2   Бумага А4,    склад Центральный,  +20
   └── строка 3   Ручка синяя,  склад Запасной,     +10
        ↑ различаются документом и номером строки, а не координатами
```

Обратите внимание на первую и третью строки: товар один и тот же. По одним и тем
же координатам движений может быть сколько угодно — приход, расход, снова приход.
Это нормально и ошибкой не считается.

А вот для разрезов координаты по-прежнему главные: именно по ним платформа
складывает итоги и группирует ответы.

## 5. Сравнить два регистра — увидеть общее и разное

У нас на руках два регистра. Положим их рядом:

| | ЦеныНоменклатуры | ТоварыНаСкладах |
|---|---|---|
| тип | регистр сведений | регистр накопления |
| вид или режим | периодический, независимый | остаточный, подчинён регистратору |
| измерения | Номенклатура | Номенклатура, Склад |
| ресурсы | Цена | Количество |
| ключ записи | Номенклатура + Период | Регистратор + номер строки |
| отвечает на | «какая цена на дату» | «сколько осталось или прошло» |

**Общее (внешний контур устройства):**

- Оба живут в ветке «Регистры», просто в разных поддеревьях.
- У обоих внутри стоят **Измерения** и **Ресурсы**. Внешне поля выглядят
  одинаково.
- Оба пока пусты — потому что никто туда ещё не писал:

```bsl,набросок
ЦеныНоменклатуры          ТоварыНаСкладах
(нет записей)             (нет записей)
```

  Регистр сам по себе ничего не порождает.
- Из обоих будем читать **не напрямую**, а через виртуальные таблицы, которые
  платформа сама построит над ними. Это § 2.5.

**Разное — в таблице выше.** Главное: у регистра сведений ключ собирается из
измерений и периода, поэтому в него можно писать напрямую; у регистра накопления
ключ держится на регистраторе, поэтому без документа записи не появится.

Заметьте: «остаточный/оборотный» у регистра накопления — это про **вид регистра** (свойство в
конфигураторе); «независимый/подчинённый регистратору» у регистра сведений — это про **режим
записи** (тоже свойство). Это два разных свойства у двух разных типов. Они не
параллельны и не аналогичны — у каждого характера регистра свой набор настроек.

## 6. Пока это только форма

Оба регистра созданы, но пусты. Ни одной строчки кода, ни одного движения, ни
одного запроса — только структура: какие колонки и какого типа. Регистр в этом
состоянии уже полноценный объект конфигурации, просто ему нечего отдать: спросить
у него остаток или срез можно, а в ответ придёт пустота.

И ещё одно, что стоит заметить сразу: наполняются эти два регистра **по-разному**.
В регистр накопления записи придут от документа, когда мы его проведём. А в
независимый регистр сведений их кладут либо руками через форму списка в режиме
Предприятия, либо обработкой загрузки — прайс-лист обычно приходит файлом, а не
рождается из документа.

## Главное в одном абзаце
В конфигураторе регистры лежат в отдельной ветке, разбитой по четырём характерам.
Мы завели два самых простых: один хранит цену на дату, другой считает, сколько
товара на складе. Каждый создаётся щелчком в дереве и заполнением того же устройства —
координаты, числа, пометки. Разница между ними в двух свойствах: первый принимает
записи откуда угодно, второй — только от документа, и потому у них по-разному
устроено то, что делает запись неповторимой. Пока оба пусты: они знают свою форму,
но содержания у них нет. Оно появится, когда документ проведётся и оставит первые
следы.

---

*Мы только что разложили поля по трём ролям почти на автомате. Но за каждым
решением «это измерение, это реквизит» стоит мини-урок, который в реальной
работе экономит часы. Именно об этом — § 2.2: «Измерение или реквизит?».*

## Контрольные вопросы

1. На какие четыре поддерева делится ветка «Регистры» в конфигураторе? Какие два из
   них мы открываем во второй части и почему?
2. Какие свойства мы задали у регистра сведений «ЦеныНоменклатуры», чтобы он стал «фотоальбомом с
   подписями»? И что у него получится ключом уникальности записи?
3. Какое свойство у регистра накопления отвечает за то, остаточный он или оборотный? Где это
   свойство в конфигураторе?
4. Что общее у регистра сведений и регистра накопления на уровне устройства и что разное на уровне ключей записи?
5. Почему во второй части мы не открываем ветки «Регистры бухгалтерии» и «Регистры
   расчёта», даже если их видно в дереве?

## Упражнения

### Повторить по образцу

Если есть пустая база — повтори создание двух регистров: `ЦеныНоменклатуры` (регистр сведений,
периодический, независимый) и `ТоварыНаСкладах` (регистр накопления, вид «Остатки»). Сравните поля,
которые конфигуратор просит заполнить у первого и второго. Какие свойства есть
**только у регистра сведений**? Какие — **только у регистра накопления**?

### Расширить регистр

К `ЦеныНоменклатуры` нам захотелось добавить **тип цены** (опт, розница, акция).
В какую часть устройства — измерения, ресурсы или реквизиты — вы бы это положили?
Измерение, напомним, делает две вещи: входит в адрес записи и служит осью разреза
при чтении. Отвечайте на интуиции — разбор этого вопроса ещё впереди.

### Ловушка

Один начинающий завёл регистр накопления «ВзаиморасчётыСКонтрагентами» с измерениями «Контрагент»
и «Валюта», ресурсом «Сумма». А вид регистра выбрал — «Обороты». Когда дошёл до
отчёта «текущий долг по каждому контрагенту», обнаружил, что не может получить
такой ответ — система не даёт. В чём ошибка с точки зрения выбора **вида регистра**?

### Чужая форма

Вам показали кусок чужой конфигурации. В ней регистр сведений со свойствами:

```текст
ЦенникРозничный
  Периодичность: Месяц
  Режим записи: Подчинение регистратору
  Измерения:
    - Номенклатура
    - МагазинСети
  Ресурсы:
    - ЦенаСНДС
  Реквизиты:
    - ИсточникЦены
```

По одной структуре, не зная кода:

1. Что значит «Подчинение регистратору» у этого регистра сведений, и чем он будет отличаться от
   нашего независимого?
2. Зачем «МагазинСети» вынесли в измерения, а «ИсточникЦены» оставили в реквизитах?
   Отвечайте на догадку.

## Ответы (три уровня)

**Уровень 0 — на пальцах.**
Регистр — это объект конфигурации, который сначала **создают** в дереве, потом
**заполняют** через документы. В этом параграфе мы только создали — задали форму:
какие колонки и какого типа. Дальше будем учиться её наполнять и спрашивать.

**Уровень 1 — основной.**

1. Сведений, накопления, бухгалтерии, расчёта. Во второй части открываем сведений и
   накопления, потому что они **проще** и не требуют бухгалтерского словаря.
   Бухгалтерия и расчёт — третья часть.
2. Периодичность «День» (чтобы у каждой записи был период-дата) + режим записи
   «Независимый» (чтобы данные клались напрямую, без документа). Ключ
   уникальности — пара **«Номенклатура × Период»**: на одну дату по одному
   товару не больше одной записи.
3. Свойство называется **«Вид регистра»** и стоит в свойствах самого регистра
   накопления. У него два значения — «Остатки» (остаточный) и «Обороты»
   (оборотный).
4. Общее: и там и там в устройстве есть измерения и ресурсы. Разное: у регистра сведений ключ
   записи — это сами измерения (плюс период у периодического), у регистра накопления ключ — пара
   «Регистратор + Номер строки», а измерения играют только роль координат
   разреза.
5. Бухгалтерия и расчёт требуют чужих слов — счетов, проводок, периодов
   действия, графиков. Это перегруз там, где мы ставим простую цель — пройти
   полный цикл «создал — записал — прочитал». Сложные характеры остаются на
   третью часть.

**Уровень 2 — экспертный конспект.**

В дереве конфигурации у регистров общий внешний контракт: имя, синоним, набор
измерений, ресурсов, реквизитов плюс тип-специфические свойства. Для регистра сведений во второй части
важны `Периодичность` (наличие поля Период и поведение среза) и `РежимЗаписи`
(независимый / подчинённый регистратору). Для регистра накопления — `ВидРегистра` (Остатки /
Обороты, влияет на наличие виртуальной таблицы Остатки) и `ПериодичностьИтогов`
(оптимизация агрегации, по умолчанию «Авто»). Идентификация записи: у
независимого регистра сведений — измерения плюс период (если периодический); у подчинённого
регистратору регистра — пара «Регистратор + НомерСтроки», где НомерСтроки —
служебное поле платформы. Свойство `Индексировать` у измерений и реквизитов
разбирается дальше, вместе с тем, как роль поля влияет на разрезы и
производительность.

**Ответ на упражнение «Расширить регистр»:** «Тип цены» в подавляющем большинстве задач —
это **измерение**. Аргумент: к нему регулярно хочется обращаться как к **оси
разреза** («какая розничная цена», «какая оптовая»), и для одной номенклатуры в
один момент времени по разным типам цены должны быть **разные записи** — то есть
тип цены попадает в ключ уникальности независимого регистра сведений. В реквизит его сделают
только если тип цены — пометка-справка, а отдельных значений по типам не
требуется.

**Ответ на ловушку (вид «Обороты»):** у регистра накопления вида «Обороты» нет виртуальной таблицы
«Остатки». Платформа специально не хранит остатки на момент по таким регистрам —
она хранит только обороты за период. Отчёт «текущий долг» — это вопрос об остатке
на конец дня, и оборотный регистр на него ответить не умеет (точнее, не сохраняет
данные так, чтобы быстро ответить). Правильное решение — поменять вид на
«Остатки». Тогда платформа начнёт хранить остатки на момент, и появится
виртуальная таблица `Остатки`.

**Ответ на чужую форму (ЦенникРозничный):**

1. «Подчинение регистратору» означает, что записи в этот регистр сведений приходят **через
   документ-регистратор**, а не напрямую. То есть для каждой записи цены в системе
   есть какой-то документ, который её внёс (например, «УстановкаЦен»). При этом
   ключ уникальности записи в подчинённом регистре сведений складывается из
   **регистратора, периода и измерений** — то есть регистратор входит в ключ,
   в отличие от независимого, где его нет вовсе. Отличие от нашего
   независимого `ЦеныНоменклатуры` — не в ключе, а **в способе записи**: у нашего
   значения кладутся напрямую, у этого — всегда через какой-то документ.
2. «МагазинСети» вынесли в измерения потому, что **по нему регулярно нужно
   нарезать данные** («цены в этом магазине», «цены во всех магазинах»). Плюс по
   нему ведётся **разная цена на один и тот же товар в разных магазинах**, значит,
   он должен быть в ключе. «ИсточникЦены» — это пометка к конкретной записи
   («откуда мы взяли эту цену»: ручной ввод, импорт, синхронизация). По нему
   обычно не группируют итоги, и для одного товара в одном магазине разные
   источники не означают разные цены — это просто справка. Поэтому он в
   реквизитах.
